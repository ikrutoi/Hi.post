import type { CardActionsState } from '../constants'

export const initialCardActionsState: CardActionsState = {
  addCart: true,
  save: true,
  remove: true,
}

export const initialMemoryCardtextState = {
  cardtext: null,
}

export const initialMinimizeState = false

export const initialFullCardState = false
