/**
 * IndexedDB record id for the single working image in the `userImages` object store
 * (adapter field: `storeAdapters.editorImages`).
 */
export const CURRENT_EDITOR_IMAGE_ID = 'current_editor_image' as const
