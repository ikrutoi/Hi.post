// import { useToolbarButtons } from '../../hooks/useToolbarButtons'
// // import { addIconToolbar } from '../../utils/'
// // import { addIconToolbar } from '../../utils/icons'

// const MyToolbar: React.FC = () => {
//   const buttons = useToolbarButtons()

//   return (
//     <div className="toolbar">
//       {buttons.map((btn) => (
//         <button key={btn.name} title={btn.tooltip}>
//           {addIconToolbar(btn.icon)}
//           {btn.showCounter && <span className="counter">3</span>}
//         </button>
//       ))}
//     </div>
//   )
// }

// export default MyToolbar
