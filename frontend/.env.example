# Auth mode: "mock" (localStorage) or "http" (Laravel API)
VITE_AUTH_MODE=mock

# Optional API base URL. Leave empty when using Vite dev proxy (/api -> backend).
# VITE_API_BASE_URL=http://localhost:8000
